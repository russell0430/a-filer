{
  "buildCommand": "pnpm run generate & pnpm run serve",
  "builds": [
    {
      "src": "./index.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/"
    }
  ]
}
