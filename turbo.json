{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "web#build": { "dependsOn": ["api#build"], "outputs": ["dist/web/**"] },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/api/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
